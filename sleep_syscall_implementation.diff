diff --git a/nachos-project-master/code/lib/list.h b/nachos-project-master/code/lib/list.h
index 3ce9553..a753d12 100644
--- a/nachos-project-master/code/lib/list.h
+++ b/nachos-project-master/code/lib/list.h
@@ -71,7 +71,7 @@ class List {
     void SelfTest(T *p, int numEntries);
     // verify module is working
 
-   protected:
+//   protected:
     ListElement<T> *first;  // Head of the list, NULL if list is empty
     ListElement<T> *last;   // Last element of list
     int numInList;          // number of elements in list
diff --git a/nachos-project-master/code/machine/interrupt.cc b/nachos-project-master/code/machine/interrupt.cc
index 4ed8291..7f76f1e 100644
--- a/nachos-project-master/code/machine/interrupt.cc
+++ b/nachos-project-master/code/machine/interrupt.cc
@@ -158,6 +158,11 @@ void Interrupt::OneTick() {
                                  // (interrupt handlers run with
                                  // interrupts disabled)
     CheckIfDue(FALSE);           // check for pending interrupts
+     bool someThreadWoken = kernel->scheduler->checkBlockedList(kernel->stats->totalTicks, false);                                                                         if(someThreadWoken) {                                                                      cout << "Yes !! Found a thread woken when running:" <<kernel->currentThread->getName()<< endl;
+            status = SystemMode;
+            kernel->currentThread->Yield();
+            status = oldStatus;
+    }
     ChangeLevel(IntOff, IntOn);  // re-enable interrupts
     if (yieldOnReturn) {         // if the timer device handler asked
                                  // for a context switch, ok to do it now
diff --git a/nachos-project-master/code/test/Makefile b/nachos-project-master/code/test/Makefile
index 396e838..6c29826 100644
--- a/nachos-project-master/code/test/Makefile
+++ b/nachos-project-master/code/test/Makefile
@@ -112,7 +112,7 @@ ifeq ($(hosttype),unknown)
 PROGRAMS = unknownhost
 else
 # change this if you create a new test program!
-PROGRAMS = abs add add2 halt shell matmult sort segments test_syscall num_io char_io random str_io ascii bubblesort help create_file open_file readwrite exec test_sem sinhvien voinuoc main
+PROGRAMS = abs add add2 sleep  halt shell matmult sort segments test_syscall num_io char_io random str_io ascii bubblesort help create_file open_file readwrite exec test_sem sinhvien voinuoc main
 endif
 
 all: $(PROGRAMS)
@@ -145,6 +145,13 @@ add2.o: add2.c
 add2: add2.o start.o
 	$(LD) $(LDFLAGS) start.o add2.o -o add2.coff
 	$(COFF2NOFF) add2.coff add2
+sleep.o: sleep.c
+	$(CC) $(CFLAGS) -c sleep.c
+
+sleep: sleep.o start.o
+	$(LD) $(LDFLAGS) start.o sleep.o -o sleep.coff
+	$(COFF2NOFF) sleep.coff sleep
+
 shell.o: shell.c
 	$(CC) $(CFLAGS) -c shell.c
 shell: shell.o start.o
diff --git a/nachos-project-master/code/threads/scheduler.cc b/nachos-project-master/code/threads/scheduler.cc
index 01f8a50..9610e62 100644
--- a/nachos-project-master/code/threads/scheduler.cc
+++ b/nachos-project-master/code/threads/scheduler.cc
@@ -23,6 +23,16 @@
 #include "scheduler.h"
 #include "main.h"
 #include "math.h"
+class WakeUpCallback : public CallBackObj {
+	public:
+		void CallBack() {
+			kernel->scheduler->checkBlockedList(kernel->stats->totalTicks, false);
+		}
+};
+SleepingThread::SleepingThread(Thread* thread, int time){
+	sleptThread = thread;
+	whenToWake = time;
+}
 //----------------------------------------------------------------------
 // Scheduler::Scheduler
 // 	Initialize the list of ready but not running threads.
@@ -33,6 +43,7 @@ int compare(Thread *t1,Thread *t2){
 }
 Scheduler::Scheduler() {
     readyList = new SortedList<Thread *>(compare);
+    blockedList = new List<SleepingThread*>;
     toBeDestroyed = NULL;
 }
 
@@ -59,6 +70,56 @@ void Scheduler::ReadyToRun(Thread *thread) {
     readyList->Insert(thread);
 }
 
+void Scheduler::pushIntoBlockedList(Thread* thread, int time) {
+	if(thread == NULL){
+		cout << "Thread passed for pushing into blockedList is NULL"<<endl;
+		ASSERT(thread != NULL);
+	}
+
+	thread->setStatus(BLOCKED);
+	IntStatus oldLevel = kernel->interrupt->SetLevel(IntOff);
+
+	SleepingThread* goingToSleep = new SleepingThread(thread, time);
+        blockedList->Append(goingToSleep);
+
+	WakeUpCallback* wcb = new WakeUpCallback();
+
+	kernel->interrupt->Schedule(wcb, time, TimerInt);
+	Thread* nextThread;
+	while((nextThread = this->FindNextToRun()) == NULL){
+		//this->checkBlockedList(kernel->stats->totalTicks, true);
+		kernel->interrupt->Idle();
+	}
+	this->Run(nextThread, FALSE);
+//	readyList->Remove(thread);
+	kernel->interrupt->SetLevel(oldLevel);
+}
+bool Scheduler::checkBlockedList(int currentTime, bool advanceTime) {
+
+	if(advanceTime){
+		kernel->stats->totalTicks += 1;
+		//cout<<"Advancing the clock"<<endl;
+	}
+	//cout<<"calling check inside blocked list"<<endl;
+	ListElement<SleepingThread*> *ptr;
+	bool someThreadWoken = false;
+
+	for (ptr = blockedList->first; ptr != NULL; ptr = ptr->next) {
+		//cout<<"";
+		cout<<"Thread to wake up at:"<<ptr->item->whenToWake<<"while current is at:"<<kernel->stats->totalTicks<<endl;
+		volatile int currentTicks = kernel->stats->totalTicks;
+
+		if(ptr->item->whenToWake <= currentTicks) {
+			SleepingThread* toBeWoken = ptr->item;
+			blockedList->Remove(toBeWoken);
+			readyList->Insert(toBeWoken->sleptThread);
+			toBeWoken->sleptThread->setStatus(READY);
+		//	cout << "Woken up a thread with name:" << toBeWoken->sleptThread->getName()<<endl;
+			someThreadWoken = true;
+		}
+	}
+	return someThreadWoken;
+}
 //----------------------------------------------------------------------
 // Scheduler::FindNextToRun
 // 	Return the next thread to be scheduled onto the CPU.
diff --git a/nachos-project-master/code/threads/scheduler.h b/nachos-project-master/code/threads/scheduler.h
index d522c87..df2e7d6 100644
--- a/nachos-project-master/code/threads/scheduler.h
+++ b/nachos-project-master/code/threads/scheduler.h
@@ -17,6 +17,12 @@
 // the data structures and operations needed to keep track of which
 // thread is running, and which threads are ready but not running.
 
+class SleepingThread {
+        public:
+                SleepingThread(Thread* thread, int time);
+                Thread* sleptThread;
+                int whenToWake;
+};
 class Scheduler {
    public:
     Scheduler();   // Initialize list of ready threads
@@ -32,10 +38,14 @@ class Scheduler {
                                 // running needs to be deleted
     void Print();               // Print contents of ready list
     Thread* PeekNextToRun();
+    bool isBlockedListEmpty();
+    void pushIntoBlockedList(Thread* thread, int time);
+    bool checkBlockedList(int currentTime, bool advanceTime);
 
     // SelfTest for scheduler is implemented in class Thread
 
    private:
+    List<SleepingThread*>* blockedList;
     SortedList<Thread*>* readyList;  // queue of threads that are ready to run,
                                // but not running
     Thread* toBeDestroyed;     // finishing thread to be destroyed
diff --git a/nachos-project-master/code/userprog/exception.cc b/nachos-project-master/code/userprog/exception.cc
index 88f3778..ca151d5 100644
--- a/nachos-project-master/code/userprog/exception.cc
+++ b/nachos-project-master/code/userprog/exception.cc
@@ -168,7 +168,11 @@ void handle_SC_Add() {
     return move_program_counter();
 }
 
-
+void handle_SC_Sleep2() {
+	int timeReq = (int)kernel->machine->ReadRegister(4);
+	SysSleep2(timeReq);
+	return move_program_counter();
+}
 
 
 
@@ -447,6 +451,8 @@ void ExceptionHandler(ExceptionType which) {
                     return handle_SC_Halt();
                 case SC_Add:
                     return handle_SC_Add();
+		case SC_Sleep2:
+		    return handle_SC_Sleep2();
                 case SC_ReadNum:
                     return handle_SC_ReadNum();
                 case SC_PrintNum:
diff --git a/nachos-project-master/code/userprog/ksyscall.h b/nachos-project-master/code/userprog/ksyscall.h
index 7865c03..6ab9017 100644
--- a/nachos-project-master/code/userprog/ksyscall.h
+++ b/nachos-project-master/code/userprog/ksyscall.h
@@ -23,6 +23,10 @@ int SysAdd(int op1, int op2) { return op1 + op2; }
 
 int SysAbs(int op1) {if(op1>0) return op1; else return -op1;}
 
+void SysSleep2(int time){
+	int whenToWake = kernel->stats->totalTicks + time;
+	kernel->scheduler->pushIntoBlockedList(kernel->currentThread, whenToWake);
+}
 int SysReadNum() {
     readUntilBlank();
 
diff --git a/nachos-project-master/code/userprog/syscall.h b/nachos-project-master/code/userprog/syscall.h
index 881ba8d..fe79227 100644
--- a/nachos-project-master/code/userprog/syscall.h
+++ b/nachos-project-master/code/userprog/syscall.h
@@ -52,7 +52,7 @@
 #define SC_Signal 53
 #define SC_GetPid 54
 #define SC_Abs 55
-
+#define SC_Sleep2 56
 
 
 #ifndef IN_ASM
@@ -73,6 +73,7 @@ void Halt();
 /*
  * Add the two operants and return the result
  */
+void Sleep2(int time);
 int Abs(int op1);
 
 int Add(int op1, int op2);
